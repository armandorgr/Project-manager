# Springboot Project Manager Backend

## Environment variables set up

### Create and update ``.env`` and ``.env.test`` files based on the included ``.env.template`` file
```properties
DB_URL= "YOUR DATABASE URL"
DB_USERNAME= "YOUR DATABASE USERNAME"
JWT_SECRET= "YOUR SECRET KEY"

# Docker compose related vars
DB_PASSWORD= "YOUR DATABASE PASSWORD"
DB_NAME= "DB NAME"
DB_PASSWORD_TEST= "TEST DATABASE PASSWORD"
DB_NAME_TEST = "TEST DATABASE NAME"

```
